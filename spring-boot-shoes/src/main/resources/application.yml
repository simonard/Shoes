server:
  port: 9033
  
spring:
  datasource:
    url: <postgres url>
    password: <password>
    username: <username>
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: create
